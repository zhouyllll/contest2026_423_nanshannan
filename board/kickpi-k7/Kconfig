
config KICKPI_K7_IMGPROC
	bool "去马赛克 + JPEG 编码"
	default n
	depends on LIB_JPEG_TURBO
	---help---
		把传感器出的 Bayer RAW12 转成 RGB 并编码为 JPEG。

		★ 逐行处理：1932x1096 的整帧 RGB888 是 6.35MB，而 ai_agent
		  跑起来后堆只剩约 6.1MB，整帧转换分配不出来。所以每次只
		  去马赛克一行立刻喂给 libjpeg，峰值内存是一行 5.8KB。

		提供 kickpi_imgproc_selftest()，用合成的红/绿/蓝三色带
		Bayer 图验证通路 —— 摄像头接不上时也能验相位映射和跨距索引。

config KICKPI_K7_BAYER_PHASE
	int "Bayer 相位（0=RGGB 1=GRBG 2=GBRG 3=BGGR）"
	default 2
	range 0 3
	depends on KICKPI_K7_IMGPROC
	---help---
		(0,0) 处是哪个颜色，取决于传感器的裁剪起点是奇是偶。

		★ 已上板实测确定为 2（GBRG）。用手机满屏纯红贴住镜头，
		  `cam cap` 后 `cam bayer` 读四个位置的平均值：

		      (0,0)=8666  (1,0)=3998
		      (0,1)=14378 (1,1)=8649

		  两个 G 相等且在主对角线（8666/8649）；红光下 (0,1) 最亮
		  是 R、(1,0) 最暗是 B —— 三条判据互相印证，排布是 GBRG。

		★ 中性场景下测不出来：四个值只差 1~3%，那是噪声不是信号。
		  必须用饱和的单色画面，手机自发光比红纸可靠（纸是反射光，
		  环境光一混就不纯）。

config KICKPI_K7_JPEG_QUALITY
	int "JPEG 质量 (1-100)"
	default 85
	range 1 100
	depends on KICKPI_K7_IMGPROC
