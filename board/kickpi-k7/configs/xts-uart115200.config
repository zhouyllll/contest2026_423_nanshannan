# xTS 1.3.11（UART 文件传输）临时叠加：控制台降到 115200。
# 用法：cat 本文件 >> nuttx/.config && make olddefconfig
#
# 1.5Mbaud 下主机侧（CH340）丢 7~16% 字节，板→主机方向的 ymodem 必然失败；
# 115200 下零丢失。测完这一项应刷回正常镜像（控制台 1500000，与 U-Boot 一致）。
CONFIG_16550_UART0_BAUD=115200
