#
# For a description of the syntax of this configuration file,
# see the file kconfig-language.txt in the NuttX tools repository.
#

config LVX_USE_DEMO_CONTEST2026_423_V4L2CAP
	bool "Contest 2026 team 423 v4l2cap"
	default n
	---help---
		Capture one JPEG frame from /dev/video0 using the exact V4L2
		call sequence that ai_agent's camera tool uses.
