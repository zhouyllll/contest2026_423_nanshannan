# 本仓库位于 openvela 工作区内（<工作区>/contest/），工作区本身由 repo 管理、
# 不属于本仓库，无需在此忽略。

# 日志工具的本地缓存（logs/ 本身要入库）
logs/.last_push
logs/.cache

# 构建产物
*.bin
*.img
*.elf
*.hex

# 厂商文档（含版权，不纳入版本控制）
*.docx
*.pdf
*.zip

# 厂商资料（含版权，仅本地参考，不纳入版本控制）
docs/vendor/

# 编译产物（板级源码目录会被 nuttx 构建系统就地编译）
*.o
*.a
*.d

# AI Coding 日志：只提交白名单内的会话（见 scripts/collect-logs.sh）
# 误跑 export-session.py --backfill 会导入全部个人项目会话，这里不做忽略，
# 而是靠 collect-logs.sh 的白名单控制；提交前请 git status 确认文件数。
