# 1.3.10 UART：2026-09-19

CH340 /dev/ttyUSB0，1500000，8N1；板端 /dev/ttyS0。
用户指示跳过未连接MPU6050的1.3.7，继续后续项。
本轮未更改驱动、未刷机。运行版本沿用本轮GPIO检查：
NuttX 3b51eaa1-dirty Sep19 2026 00:21:06。

源码默认-n0仅测试write返回字节数，不能用旧的默认1/1代替完整收发。
本轮按程序支持的-n0/-n1/-n2分别执行，串口命令和数据均保存。

| 子项 | 证据 | 判定 |
|---|---|---|
| -n0发送 | CMocka 1/1；实际数据被[CPU0]日志插入、部分字符不完整，主机未找到完整预期字符串 | 严格发送验证未通过 |
| -n1接收 | 主机发送源码DEFAULT_CONTENT逐字节5ms间隔，无附加换行；板端字符串比较成功、1/1 | 本次接收成功；串口汇总文本有缺字 |
| -n2分段回传 | 主机发送18#UART-xts-0123456789，回收匹配后才发送pass#0#；1/1并返回NSH | 18字节回传成功 |

整项不计严格PASS：发送方向尚无完整有效内容证据，不能用程序write成功
替代PC接收一致。下一步隔离测试载荷与SYSLOG输出通道后重测发送方向。
以上测试与指南手动粘贴步骤也存在程序版本差异，本次按现有实现协议执行。

case-N.raw为完整收到的原始字节，case-N-sent.bin为测试启动后发送数据，
命令见events.jsonl。capture.py为本轮执行脚本，burst-echo.raw为独立回传。
SD复测仍暂停，GPIO失败保留，未重复执行其他用例。
