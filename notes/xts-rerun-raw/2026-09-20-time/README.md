# xTS 1.3.14 时间一致性：**未完成，截止日前时间不够**（2026-09-20）

- 开始：2026-09-20 ~02:36 UTC（北京时间 10:36），跑满 24h+5min，预计 2026-09-21 ~02:41 UTC 结束。
- 镜像：ecc7cba 正常镜像（`ed8f69ad-dirty Sep 20 2026 07:35:35`）。
- 前提"未配网/无 NTP"：镜像**没有编入任何 NTP 客户端**（`CONFIG_NETUTILS_NTPCLIENT`、
  `CONFIG_SYSTEM_NTPC` 均未设置），网线接着也不会自动对时；板子时间只来自 HYM8563 + 系统时钟。
- 步骤：`date -u -s "<主机 UTC>"` 对时 → 每 10 分钟取一次样 → 6/12/18/24h 四个点标为 `CHECK-Nh`。
- 判据：终点板子时间与主机时间差 ≤ 2s。

## 与原文的差别（两处，均记录在案）

1. **取样走网络 NSH，不用 minicom 时间戳**：上一轮（09-14 起）串口在约 15h 断连，整轮作废；
   本轮串口空着，可并行用 minicom 观察。
2. **每 10 分钟多采一次**（原文只要 4 次）：中途若中断，仍有部分证据。

## 亚秒精度怎么来的

`date -u +%s` 只有整秒。每次取样连续轮询，捕捉板子秒数**跳变**的瞬间——那一刻板子正好
走到整秒边界，用此刻的主机时间对齐，偏差 = 板子整秒值 − 主机时刻，精度取决于跳变窗口
（`edge_window_s`，实测约 0.25s）。没抓到跳变时退化为整秒比较，记 `mode: coarse`。

## 结论：未完成

原文判据是"静置 **24 小时以上**后与 PC 时间差 ≤2s"。本轮 09-20 10:36 才起跑，
**24 小时终点落在截止日之后**，因此本项按未达成提交，不做任何推测性判定。
采样仍在继续，`events.jsonl` 会留下跑到多少算多少的部分数据。

### 已有的部分数据（不足以判定）

| 采样 | 偏差（板子 − 主机） | 说明 |
|---|---|---|
| `t0`（设时后即刻） | +0.227 s | **不可信**：`date -s` 把秒相位截断，设时后的第一个秒跳变不是完整的一秒 |
| `0.2h` | −1.000 s | |
| `0.3h` | −0.981 s | 与上一次相隔 599 s，偏差只变 0.019 s |

单看最后两点是 ~32 ppm（折合约 2.7 s/天），但两点各自的分辨率是 ±0.26 s
（轮询窗口），**误差比信号还大**，用两点估漂移速率没有意义 —— 这恰恰是原文
要求跑满 24 小时的原因：只有把时间拉长，漂移才会显著超过测量噪声。

上一轮（09-14 起）同样没走到终点：串口在约 15h 断连，整轮作废。
